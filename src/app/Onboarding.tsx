import { Text, View } from "react-native";

const Onboarding = () => {
  return (
    <View className="bg-red-600">
      <Text className="text-red-600 text-6xl font-bold">Onboarding</Text>
    </View>
  );
};

export default Onboarding;
